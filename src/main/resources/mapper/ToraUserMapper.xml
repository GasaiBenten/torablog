<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wanghl.torablog.mapper.ToraUserMapper">

    <update id="refreshSign">
        update tora_user a set a.is_signed = 0 where a.id = #{user.id}
    </update>
</mapper>
